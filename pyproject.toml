[project]
name = "wifi-controller-data-pipeline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "sqlalchemy>=2.0.0",
    "python-dotenv>=1.0.0",
    "psycopg2-binary>=2.9.0",
    "google-generativeai>=0.8.0",
]

[tool.vercel]
entrypoint = "app:app"
